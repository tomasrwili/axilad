apiVersion: v1
kind: Service
metadata:
  name: rjhtjo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rjhtjo"
    port: 8080
  selector:
    app: rjhtjo
