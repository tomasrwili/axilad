apiVersion: v1
kind: Service
metadata:
  name: kknzk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kknzk"
    port: 8080
  selector:
    app: kknzk
