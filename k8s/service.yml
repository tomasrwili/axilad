apiVersion: v1
kind: Service
metadata:
  name: nieim
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "nieim"
    port: 8080
  selector:
    app: nieim
