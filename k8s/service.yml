apiVersion: v1
kind: Service
metadata:
  name: bashe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bashe"
    port: 8080
  selector:
    app: bashe
