apiVersion: v1
kind: Service
metadata:
  name: hertoo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hertoo"
    port: 8080
  selector:
    app: hertoo
