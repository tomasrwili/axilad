apiVersion: v1
kind: Service
metadata:
  name: mbjgt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mbjgt"
    port: 8080
  selector:
    app: mbjgt
