apiVersion: v1
kind: Service
metadata:
  name: weyesk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "weyesk"
    port: 8080
  selector:
    app: weyesk
