apiVersion: v1
kind: Service
metadata:
  name: fdhjk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fdhjk"
    port: 8080
  selector:
    app: fdhjk
