apiVersion: v1
kind: Service
metadata:
  name: kyent
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "kyent"
    port: 8080
  selector:
    app: kyent
