apiVersion: v1
kind: Service
metadata:
  name: lbhds
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lbhds"
    port: 8080
  selector:
    app: lbhds
