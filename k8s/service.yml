apiVersion: v1
kind: Service
metadata:
  name: ttghg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ttghg"
    port: 8080
  selector:
    app: ttghg
