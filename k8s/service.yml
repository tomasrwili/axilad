apiVersion: v1
kind: Service
metadata:
  name: dafg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dafg"
    port: 8080
  selector:
    app: dafg
