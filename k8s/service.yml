apiVersion: v1
kind: Service
metadata:
  name: bencr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bencr"
    port: 8080
  selector:
    app: bencr
