apiVersion: v1
kind: Service
metadata:
  name: hdgx
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hdgx"
    port: 8080
  selector:
    app: hdgx
