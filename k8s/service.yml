apiVersion: v1
kind: Service
metadata:
  name: horsx
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "horsx"
    port: 8080
  selector:
    app: horsx
