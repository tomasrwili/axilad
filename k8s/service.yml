apiVersion: v1
kind: Service
metadata:
  name: jkhyf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jkhyf"
    port: 8080
  selector:
    app: jkhyf
