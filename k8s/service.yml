apiVersion: v1
kind: Service
metadata:
  name: trthu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "trthu"
    port: 8080
  selector:
    app: trthu
