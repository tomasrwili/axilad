apiVersion: v1
kind: Service
metadata:
  name: jrigr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jrigr"
    port: 8080
  selector:
    app: jrigr
