apiVersion: v1
kind: Service
metadata:
  name: majim
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "majim"
    port: 8080
  selector:
    app: majim
