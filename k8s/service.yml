apiVersion: v1
kind: Service
metadata:
  name: joht
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "joht"
    port: 8080
  selector:
    app: joht
