apiVersion: v1
kind: Service
metadata:
  name: dorler
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dorler"
    port: 8080
  selector:
    app: dorler
