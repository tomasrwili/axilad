apiVersion: v1
kind: Service
metadata:
  name: ythtry
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ythtry"
    port: 8080
  selector:
    app: ythtry
