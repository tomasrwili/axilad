apiVersion: v1
kind: Service
metadata:
  name: linew
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "linew"
    port: 8080
  selector:
    app: linew
