apiVersion: v1
kind: Service
metadata:
  name: admi
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "admi"
    port: 8080
  selector:
    app: admi
