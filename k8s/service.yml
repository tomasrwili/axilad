apiVersion: v1
kind: Service
metadata:
  name: obhfs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "obhfs"
    port: 8080
  selector:
    app: obhfs
