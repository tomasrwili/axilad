apiVersion: v1
kind: Service
metadata:
  name: ljhfcs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ljhfcs"
    port: 8080
  selector:
    app: ljhfcs
