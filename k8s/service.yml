apiVersion: v1
kind: Service
metadata:
  name: ringt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ringt"
    port: 8080
  selector:
    app: ringt
