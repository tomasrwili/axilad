apiVersion: v1
kind: Service
metadata:
  name: nbror
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "nbror"
    port: 8080
  selector:
    app: nbror
