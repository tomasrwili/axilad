apiVersion: v1
kind: Service
metadata:
  name: milsy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "milsy"
    port: 8080
  selector:
    app: milsy
