apiVersion: v1
kind: Service
metadata:
  name: ythgrg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ythgrg"
    port: 8080
  selector:
    app: ythgrg
