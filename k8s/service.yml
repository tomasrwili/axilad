apiVersion: v1
kind: Service
metadata:
  name: hcegd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hcegd"
    port: 8080
  selector:
    app: hcegd
