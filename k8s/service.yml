apiVersion: v1
kind: Service
metadata:
  name: ljgda
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ljgda"
    port: 8080
  selector:
    app: ljgda
