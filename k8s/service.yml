apiVersion: v1
kind: Service
metadata:
  name: lszx
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lszx"
    port: 8080
  selector:
    app: lszx
