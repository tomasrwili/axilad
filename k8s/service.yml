apiVersion: v1
kind: Service
metadata:
  name: jgfjd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jgfjd"
    port: 8080
  selector:
    app: jgfjd
